PROJECT_NAME=Label Police Backend
API_V1_STR=/api/v1
<<<<<<< HEAD

# ──────────────────────────────────────────────────────────────────────────────
# Vision Provider Configuration (Multi-Provider Fallback Cascade)
# ──────────────────────────────────────────────────────────────────────────────

# Layer 1: Google Gemini (native google-genai SDK)
# Comma-separated list of Gemini API keys for cascading fallback.
# The service will try each key across multiple models (gemini-3.6-flash, gemini-2.5-flash).
GEMINI_API_KEYS=

# Legacy single-key fallback (deprecated; use GEMINI_API_KEYS for multi-key support).
GEMINI_API_KEY=

# Layer 2: OpenRouter (free tier fallback when all Gemini keys are exhausted)
# OpenRouter provides free access to vision-capable models as backup nodes.
OPENROUTER_API_KEY=

# OpenRouter models tried in cascade: google/gemma-4-31b-it:free, meta-llama/llama-3.2-11b-vision-instruct:free, openrouter/free
OPENROUTER_MODEL=google/gemma-4-31b-it:free

# Optional public URL for OpenRouter request attribution.
OPENROUTER_SITE_URL=

# Layer 3: OpenAI (paid tier, not part of default cascade)
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4o-mini

# ──────────────────────────────────────────────────────────────────────────────
# Model Configuration
# ──────────────────────────────────────────────────────────────────────────────

GEMINI_MODEL=gemini-3.6-flash

# ──────────────────────────────────────────────────────────────────────────────
# Server Configuration
# ──────────────────────────────────────────────────────────────────────────────

CORS_ORIGINS=["*"]
SECRET_KEY=replace-with-a-long-random-secret
DATABASE_URL=sqlite:///./label_police.db
=======
CORS_ORIGINS=["*"]
SECRET_KEY=replace-with-a-long-random-secret
DATABASE_URL=sqlite:///./label_police.db
MAX_UPLOAD_BYTES=10485760
VISION_REQUEST_TIMEOUT_SECONDS=45

# ---------------------------------------------------------------------------
# PRIMARY_VISION_AI — Gemini 1.5 / 2.0 Pro or Flash
# ---------------------------------------------------------------------------
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# ---------------------------------------------------------------------------
# FALLBACK_VISION_AI — GPT-4o, then Claude 3.5 Sonnet
# ---------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4o
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Optional extra vision gateway (kept for backward compatibility).
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemma-4-31b-it:free
OPENROUTER_SITE_URL=

# Comma-separated fallback order. Unconfigured providers are skipped.
VISION_PROVIDER_ORDER=gemini,openai,anthropic,openrouter,ocrspace,azure,google

# ---------------------------------------------------------------------------
# DEDICATED_OCR — OCR.space, Azure Computer Vision, or Google Cloud Vision
# ---------------------------------------------------------------------------
# OCR.space API: https://ocr.space/ocrapi (FREE - 25,000/month, no credit card)
OCRSPACE_API_KEY=

AZURE_VISION_KEY=
AZURE_VISION_ENDPOINT=https://your-resource.cognitiveservices.azure.com
GOOGLE_CLOUD_VISION_KEY=

# ---------------------------------------------------------------------------
# PRODUCT_DATABASE_API — barcode catalogs / Open Food Facts
# ---------------------------------------------------------------------------
BARCODE_LOOKUP_API_KEY=
OPEN_FOOD_FACTS_URL=https://world.openfoodfacts.org

# ---------------------------------------------------------------------------
# WEB_VERIFICATION_API — manufacturer / FSSAI lookup
# ---------------------------------------------------------------------------
TAVILY_API_KEY=
SERPAPI_KEY=
>>>>>>> origin/backendapi-pdf
